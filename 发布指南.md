# 发布指南：把 PicTrace 推送到 GitHub

> 本仓库已完成 `git init` 与首次提交，只差"创建远程仓库 + 推送"两步。

## 方式一：一键脚本（推荐）

1. 打开 <https://github.com/new>，登录 **xiaoanping-101** 账号：
   - Repository name: `pictrace`
   - Description（复制以下内容）：
     ```
     溯图 PicTrace — 开源以图溯源检索平台：上传一张图片，聚合 Google Lens / Yandex / Bing / TinEye / 百度识图 / 搜狗等 14 个引擎，定位图片在公众号文章、帖子、视频中的出处链接；附浏览器本地 EXIF 取证、感知哈希、引用报告导出。零依赖 / 中英双语 / MIT。
     ```
   - 选择 **Public**，**不要**勾选 README/.gitignore/License（本地已有）；
   - 点 **Create repository**。
2. 双击运行 `publish.bat`（Windows）或 `bash publish.sh`（macOS/Linux）：
   - 首次推送会弹出浏览器让你登录 GitHub（Git Credential Manager 完成 OAuth）；
   - 直连失败时脚本自动改走本地代理 `127.0.0.1:7897`。

## 方式二：命令行手动

```bash
cd pictrace
git remote add origin https://github.com/xiaoanping-101/pictrace.git
git push -u origin main
# 如需代理： git config http.https://github.com/.proxy http://127.0.0.1:7897
```

## 推送后建议的收尾（约 2 分钟）

1. 仓库页面右侧 **About 齿轮** → 添加 Topics：`reverse-image-search` `image-provenance` `osint` `image-forensics` `perceptual-hash` `zh-cn`；
2. 确认首页 "Cite this repository" 按钮出现（来自 `CITATION.cff`）；
3. README 中的 Mermaid 流程图会由 GitHub 自动渲染，检查显示正常；
4. （可选）Settings → Pages → 从 main 分支发布 `public/` 目录作静态演示站（无服务端聚合，其余功能可用）；
5. （可选）打标签发布版：`git tag v1.0.0 && git push --tags`。

## 网络说明（中国大陆）

- `github.com` 直连可能失败，本机可用代理 `http://127.0.0.1:7897`；
- git 已按仓库配置好代理兜底命令（见 publish.bat 第 [2/3] 步）；
- 服务端聚合要访问国际引擎时，启动方式：
  ```bash
  NODE_USE_ENV_PROXY=1 HTTPS_PROXY=http://127.0.0.1:7897 node server.js
  ```
